# ExperimentalPlatform
[Unit]
Description=NMBD Service announces the hostname to windows networks
After=network-online.target update-images-protonet.service
Requires=network-online.target
Wants=update-images-protonet.service

[Service]
TimeoutStartSec=0
TimeoutStopSec=15
Restart=always
RestartSec=60s
Type=notify
NotifyAccess=all
ExecStart=/bin/bash -c '/opt/bin/systemd-docker --cgroups name=systemd run --rm \
    --name=hostname-smb \
    --hostname=$(hostname) \
    --net=host \
  dockerregistry.protorz.net/hostname-smb:latest'

[Install]
WantedBy=multi-user.target
